name: Weather Regression Tests
on:
  push:
    branches: [main]
jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check temperature tests
        run: node tests/unit_state.js
