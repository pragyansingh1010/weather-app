# Quality Check

- Trim the location before requesting weather data.
- Show a loading state while data is being retrieved.
- Handle invalid locations without exposing raw API errors.
- Keep weather units consistent across displayed values.
- Never place private API credentials directly in client-side UI code.
