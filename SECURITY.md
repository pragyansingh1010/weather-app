# Security Notes

This project is a browser-based learning demo.

## API credentials

API credentials should not be committed to a public repository. For production use, move authenticated API requests to a server-side component and rotate any credential that has previously been exposed.

## Reporting

For a security concern, avoid publishing credentials or exploit details in a public issue.
